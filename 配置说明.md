# 程序逻辑说明

## 📖 整体流程

### 1. 主程序流程 (main_groupchat.py)

```
启动程序
   ↓
初始化 Azure OpenAI
   ↓
创建智能体（张三、李四）
   ↓
创建群聊管理器
   ↓
玩家选择装备（1-4）
   ↓
进入自动对话循环 ←─────┐
   ↓                    │
选择下一个发言者         │
   ↓                    │
智能体生成响应           │
   ↓                    │
流式输出内容（逐字显示）  │
   ↓                    │
显示 Token 消耗         │
   ↓                    │
判断是否终止？           │
   ├─ 否 ──────────────┘
   ↓ 是
显示总 Token 统计
   ↓
结束
```

### 2. 核心对话循环详解

每次循环做这些事：

#### 步骤 1：选择发言者 (SelectionStrategy)
```python
# AgentGroupChat 自动决定谁该说话
next_speaker = self.select_next_speaker()
# 例如：根据上下文，决定是张三还是李四
```

#### 步骤 2：智能体生成响应
```python
# 调用选中的智能体
response, token_usage = self.invoke_agent(next_speaker)
# 返回：响应内容 + 本次消耗的 token
```

#### 步骤 3：流式输出
```python
# 逐字显示响应（像打字机效果）
print_stream(response.content, delay=0.015)
# 每个字延迟 0.015 秒
```

#### 步骤 4：显示 Token
```python
# 显示本次对话消耗的 token
print(f"💰 Token: 输入={token_usage['prompt_tokens']}, "
      f"输出={token_usage['completion_tokens']}, "
      f"总计={token_usage['total_tokens']}")
```

#### 步骤 5：判断终止 (TerminationStrategy)
```python
# 自动判断对话是否应该结束
if self.should_terminate():
    break  # 结束循环
# 否则继续下一轮
```

---

## 🎯 关键概念

### AgentGroupChat 类

这是核心管理器，负责：

1. **管理智能体**
   - 张三（铁匠）：查询材料、打造装备
   - 李四（学徒）：查询价格、收款

2. **自动选择发言者** (SelectionStrategy)
   - 根据对话上下文，智能选择下一个该说话的人
   - 类似 C# Semantic Kernel 的 SelectionStrategy

3. **自动判断终止** (TerminationStrategy)
   - 判断对话是否应该结束
   - 只有玩家支付成功才终止
   - 取消支付不终止（让张三继续发飙）

4. **工具调用**
   - 智能体可以调用工具（查询材料、价格、收款）
   - 自动处理工具结果

5. **Token 统计**
   - 记录每次对话的 token 消耗
   - 累计总消耗

---

## 💡 实际运行示例

### 场景：玩家选择打造飞剑

```
👤 玩家: 我想打造一把飞剑

🔨 张三: [流式输出] 材料清单搞定了！制作飞剑需要...
   💰 Token: 输入=150, 输出=80, 总计=230

🔨 李四: [流式输出] 收到啦，张三大哥！我这就去准备材料...
   💰 Token: 输入=200, 输出=60, 总计=260

🔨 张三: [流式输出] 第1步：准备材料和工具！快点！
   💰 Token: 输入=250, 输出=50, 总计=300

... (继续对话)

🔨 李四: [调用工具] request_payment(飞剑, 500)
   💰 [支付系统] 是否确认支付？(y/n): y
   ✅ 支付成功！

🔨 李四: [流式输出] 太好了！感谢大侠！
   💰 Token: 输入=300, 输出=40, 总计=340

============================================================
✅ 对话结束
============================================================
📊 总 Token 消耗统计：
   输入 Token: 1500
   输出 Token: 800
   总计 Token: 2300
============================================================
```

---

## 🔧 代码关键点

### 1. 流式输出函数

```python
def print_stream(text: str, delay: float = 0.02):
    """逐字输出文本"""
    for char in text:
        sys.stdout.write(char)  # 输出一个字符
        sys.stdout.flush()      # 立即刷新显示
        time.sleep(delay)       # 延迟一点时间
    print()  # 最后换行
```

### 2. 主循环

```python
for response, token_usage in chat.run():
    # response: 智能体的响应内容
    # token_usage: 本次消耗的 token 统计
    
    # 流式输出
    print_stream(response.content)
    
    # 显示 token
    print(f"💰 Token: {token_usage}")
```

### 3. Token 统计

```python
# 每次 LLM 调用后，自动统计
if 'token_usage' in response.response_metadata:
    usage = response.response_metadata['token_usage']
    self._update_token_stats(usage)  # 累加到总统计
```

---

## ❓ 常见问题

### Q1: 为什么要流式输出？
**A:** 模拟真实打字效果，提升用户体验，看起来更自然。

### Q2: Token 统计有什么用？
**A:** 
- 了解每次对话的成本
- 优化提示词，减少 token 消耗
- 监控总消耗，控制预算

### Q3: 取消支付后会怎样？
**A:** 
- 李四会劝说玩家
- 张三会发飙骂人
- 对话继续，不会结束
- 直到玩家支付或达到最大轮数

### Q4: 如何调整流式输出速度？
**A:** 修改 `delay` 参数：
```python
print_stream(text, delay=0.01)  # 更快
print_stream(text, delay=0.03)  # 更慢
```

---

## 🚀 运行程序

```bash
# 运行主程序
python3 main_groupchat.py

# 运行演示（自动支付）
python3 demo_final.py
```

---

## 📝 总结

这个程序实现了：

✅ 多智能体自动协作（类似 C# Semantic Kernel）  
✅ 自动选择发言者（SelectionStrategy）  
✅ 自动判断终止（TerminationStrategy）  
✅ 工具调用（查询材料、价格、收款）  
✅ 流式输出（逐字显示）  
✅ Token 统计（每次 + 总计）  
✅ 取消支付后继续对话  
✅ 张三粗鲁暴躁的性格  

核心思想：**让 AI 智能体自动协作，而不是手动编排流程**。
